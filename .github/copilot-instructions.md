# Copilot Instructions

## Project-Specific Rules
- User refuses to use any external .exe files (yt-dlp, ffmpeg, etc.) placed next to the app. Only .NET libraries, NuGet packages, and DLLs are acceptable. Tools like yt-dlp can be auto-downloaded to AppData at runtime. The constraint is about what ships alongside the app, not about what gets fetched at runtime.